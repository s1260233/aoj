#include <bits/stdc++.h>
using namespace std;

int main(){

  while(1){
    int x, y;
    cin>>x>>y;

    if(x > y) swap(x, y);
    cout<<x<<" "<<y<<endl;

    if(x == 0 && y == 0) break;
  }

  return 0;
}
    
